# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Dependency directories
node_modules/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


# --- Project Specific ---
# Ignore the environment file inside the backend folder
backend/.env

# Ignore build artifacts from the backend if any
backend/dist/

# --- Bun & Lock Files ---
# Since you have bun.lockb, ignore other lockfiles to keep it clean
# (Optional: keep package-lock.json if you use both, but usually pick one)
# yarn.lock
# package-lock.json

# --- Vite/TypeScript ---
# Ignore the temporary Vite config files seen in your file list
vite.config.ts.timestamp-*
*.mjs
*.cjs

# --- Database/Supabase ---
# If you ever pull supabase local configs
.supabase/